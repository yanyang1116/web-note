import React, { useMemo, useState } from 'react';

import styles from './index.module.scss';

export default function MenuPreviewList() {
	return (
		<div className={styles.wrapper}>
			<ul>
				<li>image</li>
			</ul>
		</div>
	);
}
